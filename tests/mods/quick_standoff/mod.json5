// T3-010 test mod: the flagship AI profile with a 300-tick stand-off cap,
// so a test can watch the cap end the stand-off within a short run.
{
  id: "quick_standoff",
  name_key: "quick_standoff.mod.name",
  version: "0.1.0",
  engine_version: "*",
  dependencies: [{ id: "rome", version: ">=0.1.0" }],
}
