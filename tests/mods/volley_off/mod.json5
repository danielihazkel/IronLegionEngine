// T2-030 test mod: turns volley synchronisation off (SIM-PROJ-003) so the
// per-soldier reload path is exercised; nothing else changes.
{
  id: "volley_off",
  name_key: "volley_off.mod.name",
  version: "0.1.0",
  engine_version: "*",
  dependencies: [{ id: "rome", version: ">=0.1.0" }],
}
