# Overrides the workspace clippy.toml: the app shell owns the wall clock (frame
# timing, the fixed-step accumulator, the profiler) and the filesystem
# (scenario loading, logs). Wall-clock values never enter the sim except as
# whole ticks through `BattleWorld::step` (SIM-DET-006, SAD §6.1).

disallowed-methods = []
disallowed-types = []
