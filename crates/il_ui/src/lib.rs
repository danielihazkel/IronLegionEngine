//! Iron Legion UI (`il_ui`, TDD §11). Created in T1-050 so the app's
//! dependency graph is final; egui state and the profiler overlay arrive in
//! T1-060, input, selection and gestures in T1-061/T1-062.
